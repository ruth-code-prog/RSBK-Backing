import {View, Text} from 'react-native';
import React from 'react';

const New = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'white',
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text style={{color: 'blue'}}>Welcome</Text>
    </View>
  );
};

export default New;
